# TMDB Read Access Token (Bearer) from https://www.themoviedb.org/settings/api
# Vite only exposes variables prefixed with VITE_ to the client bundle.
# On Vercel, add VITE_TMDB_ACCESS_TOKEN (or map your AUTH_TOKEN to this name).
VITE_TMDB_ACCESS_TOKEN=
